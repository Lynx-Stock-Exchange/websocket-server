package config

// TODO: Define server configuration for the websocket service.
// Include listen address, websocket read/write deadlines, ping interval, allowed origins, and service/store connection settings.
// Keep API secrets out of config files that can be committed.
